/**
 * Topic input + five demo-topic cards + Sam intro card. Owner: C (Block C1 step 2).
 */

"use client";

export function TopicPicker() {
  // TODO(C): input, demo cards, POST /api/session, route to /session/:id
  return null;
}
