/**
 * Gemini fast-tier spike. Owner: D (Block D1 step 2c). Run standalone: npx tsx spike-gemini.ts
 * Time-to-first-token on an Evaluator-sized prompt (use fixtures/graph-tcp.json
 * as the payload). Results go in ./README.md.
 */

console.log("TODO(D): Gemini spike");
