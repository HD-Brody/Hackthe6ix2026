/**
 * B's eval harness (Block B1 step 4). Run: npm run eval
 *
 * Runs a prompt against N test inputs, dumps results side by side.
 * Crude is fine — you'll run it fifty times this weekend.
 * Never wait on the app to test a prompt; this script hits Gemini directly
 * with fixtures.
 */

// TODO(B): load test-utterances.json + a fixture graph, run evaluate() (or a
// raw prompt) over each, print utterance | verdict | quote in columns.

console.log("eval harness: not implemented yet");
