/**
 * `make demo` / npm run seed-demo. Owner: D (Block D2 step 8), driven by A at CP6.
 *
 * Seeds the demo sessions: pre-create sessions for the five cached topics in
 * fixtures/graphs/ so the live demo never waits on graph generation.
 */

console.log("TODO(D): seed demo sessions");
